secrets:
  frontend_credentials:
    # To add entires here run "htdigest -c temp-file.txt cgit [username]"
    # You will be prompted for a password on command line
    # Copy contents of the temp-file.txt to here
    - "dummy_user:cgit:ab83bafe0460f3e1b0d222a3c54e248a"
